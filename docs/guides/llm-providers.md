# LLM Providers

The CLI delegates LLM selection to each module. Set the relevant env vars in `.env`:

- `OPENAI_API_KEY` — OpenAI
- `ANTHROPIC_API_KEY` — Anthropic / Claude
- `RAGPDF_EMBEDDING_BACKEND` — embedding provider for RAG
